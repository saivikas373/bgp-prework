# Pinned for Python 3.6.8 (jump server default) - newer netmiko/pandas
# require Python 3.8+/3.9+ and won't install here.
netmiko==3.4.0
pandas==1.1.5
openpyxl==3.0.10

# Explicit pin: netmiko's unbounded "paramiko>=2.6.0" otherwise resolves to
# the latest paramiko, which needs a bcrypt version requiring a Rust build
# toolchain not available on the jump server. This version has prebuilt
# wheels for Python 3.6.
paramiko==2.11.0
bcrypt==3.2.2
